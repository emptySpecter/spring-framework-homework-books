spring:
  shell:
    interactive:
      enabled: false

  datasource:
    json: data_test.json

  jpa:
    generate-ddl: false
    #    generate-ddl: true
    hibernate:
      ddl-auto: none
    #      ddl-auto: create-drop
